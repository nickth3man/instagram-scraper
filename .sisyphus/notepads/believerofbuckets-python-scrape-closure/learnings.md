- 2026-03-10: Research refs for Tasks 1-4: Instaloader docs confirm 429 handling can sleep/retry by default, while local fail-fast script already uses sleep=False, max_connection_attempts=1, request_timeout=20.0, and a custom RateController override. Python stdlib refs most useful for closure helpers are csv + collections.Counter for exact-row duplicate scans, subprocess.run(..., timeout=...) for bounded checks, and json/pathlib/hashlib/tempfile for machine-readable evidence manifests and safe staging.
- Task 1 closure artifacts: authoritative completion sources are tool_dump.json, posts.csv, comments.csv, errors.csv, and downloads/.
- Non-authoritative completion artifacts for the current believerofbuckets tree are summary.json, checkpoint.json, errors.ndjson, and extraction_errors.json; current stale-policy classification archives browser-era leftovers and removes lock files.
- Authoritative accounting is derived from tool_dump.json + posts.csv + errors.csv only; current real counts are 99 successful shortcodes and 1 failing shortcode.
- Task 1 Ruff follow-up: public helper APIs in this repo need full docstring sections in NumPy-style format (for example Returns/------- and Raises/------), and pytest  strings should use  when messages contain punctuation.
- Task 1 Ruff follow-up: public helper APIs in this repo need full docstring sections in NumPy-style format (for example Returns/------- and Raises/------), and pytest match strings should use re.escape(...) when messages contain punctuation.
- 2026-03-10 Task 4: `uv run python -m instagram_scraper.workflows.closure_audit data/believerofbuckets` now recomputes closure counts from `tool_dump.json`, `posts.csv`, `comments.csv`, `errors.csv`, and `downloads/` only; current real output is `100` input URLs, `99` successful posts, `1` failing shortcode, `0` duplicate post rows, `286` duplicate comment rows, and `99` download bases.
- 2026-03-10 Task 4: download base counting should normalize only a trailing carousel suffix like `_2`; real shortcode stems can contain embedded underscores (for example `DU_O_tZDs00`), so stricter basename parsing will undercount or fail on valid artifacts.
- 2026-03-10 Task 3 dedupe: the believerofbuckets closure comments schema is exactly `post_shortcode,id,parent_id,created_at_utc,text,comment_like_count,owner_username,owner_id`, so exact duplicate detection must key on all eight fields in that order rather than `id` alone.
- 2026-03-10 Task 3 dedupe: current `data/believerofbuckets/comments.csv` audits to 1608 rows before dedupe, 1322 after exact-row dedupe, and 286 removed duplicates across `DU65mGajESU`, `DVFOd7RDB39`, `DVPXYYWjeaR`, `DVROBZEjn9b`, `DVTu9R5DjQo`, `DVcPTxfjQuh`, `DVd08m0juDd`, and `DVkCXZGDYzi`.
- 2026-03-10T21:03:44Z: Task 2 bounded freshness verifier lives in src/instagram_scraper/workflows/bounded_freshness.py and writes timestamped JSON evidence under .sisyphus/evidence/. It reuses the fail-fast Instaloader posture (sleep=False, max_connection_attempts=1, request_timeout bounded, custom 429 override) and pairs it with a no-retry shortcode API check so any unexpected shortcode resolution flips the run to exit code 1.
- 2026-03-10 Task 4 retry: `duplicate_comment_row_count` in `src/instagram_scraper/workflows/closure_audit.py` must reuse Task 3 exact-row semantics via the full closure comment schema; same `post_shortcode` + `id` rows with different `text` or other fields are distinct comments and must not count as duplicates.
- 2026-03-10 Task 5 cleanup: `data/believerofbuckets/` now has an unambiguous closure state with authoritative outputs left in place, stale browser-era leftovers archived under `data/believerofbuckets/archive/browser-era-stale-20260310/`, removable `.lock` clutter deleted, and bounded `DVsHusCjCTU` evidence preserved under `.sisyphus/evidence/`.
- 2026-03-10 Task 6 reconciliation: `uv run python -m instagram_scraper.workflows.comment_reconciliation data/believerofbuckets --summary-path data/believerofbuckets/comment_reconciliation_summary.json` deduped the authoritative `comments.csv` in place from 1608 rows to 1322 rows, removed 286 exact duplicate rows across `DU65mGajESU`, `DVFOd7RDB39`, `DVPXYYWjeaR`, `DVROBZEjn9b`, `DVTu9R5DjQo`, `DVcPTxfjQuh`, `DVd08m0juDd`, and `DVkCXZGDYzi`, and left 5 explicit post-level mismatches versus `posts.csv`.
- 2026-03-10 Task 6 reconciliation: the remaining post/comment deltas are `DVdsdjXjpXx` (-3), `DVcPTxfjQuh` (-1), `DVL--dEjjNh` (-2), `DVGrdRNjl8L` (-1), and `DU65mGajESU` (-17); `closure_audit` now reports `duplicate_comment_row_count = 0` on `data/believerofbuckets`.
- 2026-03-10 Task 7 closure report: `data/believerofbuckets/final_closure_report.md` summarizes the final 99+1 accounting, references the bounded freshness evidence and reconciliation summary, and points at `data/believerofbuckets/archive/browser-era-stale-20260310/` only as archived history, not as final proof.
